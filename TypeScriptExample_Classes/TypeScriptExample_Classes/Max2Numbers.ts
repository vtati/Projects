function MaxNumber(firstNum: Number, secNumber: Number): Number {
    if (firstNum >= secNumber)
        return firstNum;
    else
        return secNumber;
}

document.body.innerHTML = "Max Number is : dsfsdf" + MaxNumber(10, 20); 